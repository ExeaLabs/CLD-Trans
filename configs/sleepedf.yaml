defaults:
  - base
  - _self_

mode: stage2
data:
  synthetic: false
  dataset: sleepedf
  path: /scratch/cld-trans/datasets/sleep-edf
  sample_rate: 100.0
  num_steps: 2000
  window_seconds: 20.0
  majority_keep_ratio: 0.35
model:
  num_channels: 7
  num_classes: 5
  codebook_size: 256
  motif_dim: 64
  hidden_dim: 64
  tau_max: 2.0
  top_k: 4
  ode_solver: rk4
train:
  device: cuda
  precision: bf16
  batch_size: 160
  num_workers: 12
  prefetch_factor: 4
  persistent_workers: true
  pin_memory: true
  lr: 1.0e-4
  max_steps: null
  max_train_steps: null
  max_val_steps: null
  epochs: 8
  warmup_steps: 0
  log_interval: 50
  task_type: single_label
  val_split: 0.1
  monitor: val_loss
  early_stopping:
    enabled: true
    mode: min
    patience: 2
    min_delta: 1.0e-4
  ema:
    enabled: false
    decay: 0.999
